apiVersion: v1
kind: Service
metadata:
  name: {{ include "galene.fullname" . }}
  labels:
    {{- include "galene.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "galene.selectorLabels" . | nindent 4 }}
